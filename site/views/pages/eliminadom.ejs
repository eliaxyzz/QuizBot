<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Online - Elimina</title>
    <link rel="stylesheet" href="/CSS/style.css" type="text/css">
    <style>
        .form-card {
    background: white;
    max-width: 500px;
    margin: 40px auto;
    padding: 40px 30px;
    border-radius: 12px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.15);
    transition: transform 0.3s, box-shadow 0.3s;
}

.form-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 30px rgba(0,0,0,0.2);
}

.form-title {
    text-align: center;
    margin-bottom: 30px;
    font-size: 28px;
    color: #333;
}

.form-container label {
    display: block;
    margin-bottom: 8px;
    font-weight: 500;
    color: #555;
}

.form-container input {
    width: 70%;
    padding: 14px 15px;
    margin-bottom: 25px;
    border-radius: 8px;
    border: 1px solid #ccc;
    font-size: 16px;
    transition: 0.3s;
}

.form-container input:focus {
    border-color: #e74c3c;
    box-shadow: 0 0 8px rgba(231, 76, 60, 0.5);
    outline: none;
}

/* Bottone di eliminazione */
.btn-delete {
    width: 100%;
    padding: 14px;
    font-size: 16px;
    font-weight: 700;
    border: none;
    border-radius: 8px;
    background-color: #e74c3c;
    color: white;
    cursor: pointer;
    transition: background-color 0.3s, transform 0.2s;
}

.btn-delete:hover {
    background-color: #c0392b;
    transform: scale(1.03);
}
    </style>
</head>

<body>
    <div class="container">
        <header>
            <%- include ('../partials/header') %>
        </header>
        <div class="topnav">
            <a class="active" href="/startpage">Homepage</a>
            <a href="/domande/aggiungidom">Aggiungi</a>
            <a href="/domande/modificadom">Modifica</a>
            <div class="search-container">
                <form action="/domande/cercadom" method="GET">
                    <input type="text" placeholder="Cerca..." name="id">
                    <button type="submit">Cerca</button>
                </form>
            </div>
        </div>

        <div class="content">
    <div class="form-card">
        <h1 class="form-title">Elimina una Domanda</h1>
        <form action="/domande/eliminadom" method="POST" class="form-container">
            <label for="id">ID Domanda</label>
            <input id="id" name="id" type="number" placeholder="Inserisci l'ID" required>

            <button type="submit" class="btn-delete">Elimina</button>
        </form>
    </div>
</div>
        <footer class="footer">
            <%- include ('../partials/footer') %>
        </footer>
    </div>
</body>

</html>